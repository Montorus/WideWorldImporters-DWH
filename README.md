# WideWorldImporters-DWH
Microsoft official SQL Server OLTP sample database — normalized schema with 48 tables across 4 schemas (Application, Sales, Purchasing, Warehouse). Features Temporal tables, JSON support, In-Memory OLTP, Row-Level Security and Columnstore indexes.
